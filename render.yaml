services:
  - type: web
    name: email-assistant-backend
    runtime: java
    rootDir: email-writer
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false